name: Notify Discord

on:
  workflow_dispatch: # 手動トリガーを追加
  push:
    branches:
      - test
  pull_request:
    branches:
      - test

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.set_result.outputs.result }}
    steps:
      - name: Build and Deploy
        id: build
        run: |
          echo "ビルドとデプロイが成功しました"
          echo "success" > result.txt 

      - name: Set result variable
        id: set_result
        run: |
          result=$(cat result.txt)
          echo "::set-output name=result::$result"
  notify:
    runs-on: ubuntu-latest
    needs: build_and_deploy
    steps:
      - name: Discord Message Notify
        run: |
          if [[ "${{ needs.build_and_deploy.outputs.result }}" == "success" ]]; then
            message='{"text": "ビルドとデプロイが成功しました！ 🎉"}'
          else
            message='{"text": "ビルドとデプロイに失敗しました。エラーメッセージを確認してください。 ❌"}'
          fi
        
          # Discordにメッセージを送信
          curl -X POST -H 'Content-type: application/json' --data "$message" ${{ secrets.DISCORD_WEBHOOK_URL }}
          # run: curl -X POST -F content="hello world" ${{ secrets.DISCORD_WEBHOOK_URL }}
